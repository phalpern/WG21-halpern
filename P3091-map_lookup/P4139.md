---
title: "Better Name for Better Lookups in P3091"
document: D4139R2
# $TimeStamp:
date: 2026-05-06 17:32 EDT
# $
audience: LEWG
author:
  - name: Nathan Meyers
    email: <ncm@cantrip.org>
  - name: Pablo Halpern
    email: <phalpern@halpernwightsoftware.com>
working-draft: N5032
toc: true
toc-depth: 2
---

Abstract
========

[@P3091R5], forwarded to LWG for C++29, adds `get` member functions to
associative containers in the Standard Library. These member functions work
like `operator[]` or `at`, but return an `optional` value instead of a
reference. When a lookup fails, `get` returns a disengaged object. While the
feature itself was strongly favored, support for the name `get` yielded only a
weak consensus, with opposition to the name focusing on the fact that nowhere
else in the Standard Library can a `get` call fail. Hoping to make the C++26
cutoff, the naming discussion was kept brief and the status-quo name in P3091
was retained when that paper was forwarded in Sofia. Since that time, even the
author of P3091 has reconsidered the implications of the name `get`. As LWG has
not yet taken up the paper (which is now on track for C++29) we present an
analysis of the status quo and propose alternative names for the desired
functionality.

Change Log
==========

R2 - Pre-Brno Mailing, 2026-05
------------------------------

* Expanded discussion/motivation section
* Added description of how the names differed in the way the approach the
  naming criteria.
* Added formal wording

R1 - Post-Croydon Mailing, 2026-03-26
-------------------------------------

* Added Pablo Halpern as co-author
* Added discussion of `try_xxxxx` functions already in the Standard.
* Proposes alternatives `lookup` and `try_lookup` in addition to `try_at`.
* Add details like feature-test macro, etc.

R0 - Post-Croydon Mailing, 2026-03-23
-------------------------------------

* First version included in a mailing.


Motivation
==========

A Review of P3091
-----------------

The `[unordered_]map<Key, Value>::get` member described in [@P3091R5] would
typically appear in a context like,

```C++
if (auto maybe = container.get(key)) {
    use(*maybe);
}
```

or

```
use(container.get(key).value_or(@_some-default_@));
```

The semantics of the `get` function is that it returns an engaged
`optional<mapped_type&>` if the key is found, and a disengaged optional
otherwise.

Current Usage of `get` in the Standard Library
----------------------------------------------

The current Standard Library defines a `get` function for the following class templates:

| | |
| -------------------- | -------------------- | -------------------- |
| `array`              | `messages`           | `shared_ptr`         |
| `basic_format_args`  | `money_get`          | `subrange`           |
| `basic_istream`      | `num_get`            | `time_get`           |
| `complex`            | `pair`               | `tuple`              |
| `future`             | `reference_wrapper`  | `unique_ptr`         |
| `integer_sequence`   | `shared_future`      | `variant`            |


In reviewing these definitions, we found that:

* None of them wraps the returned object in a type having a null or error
  state, such as `optional`, `expected`, or a nullable pointer type. Note that
  `shared_ptr<>::get` and `unique_ptr::get` might return `nullptr`, but only
  when `nullptr` is the actual value stored in the smart pointer (i.e,
  returning `nullptr` is a kind of success).

* When `get` returns without throwing, the operation was successful except for
  I/O streaming-related classes (`basic_istream`, `num_get`, `money_get`, and
  `time_get`).

The I/O streaming interface predates `optional` and even largely predates
exceptions in the language. Testing the `failbit`, `errbit`, and `eol` state of
a stream after an operation is brittle and is arguably not a good model from
which to craft a modern interface.

The library has accumulated an assortment of `try_xxxxx` members that share
the feature of reporting failure, but return a variety of result types, some
presenting an iterator adjacent to where an element *would have* been inserted
or erased, or `end()`; sometimes in a pair with a boolean flag.

Decades before those, containers had `at(key)` that throws on a lookup failure,
and `operator[](key)` that insert a default-initialized value on lookup failure
and return a reference to it, but also might throw if insertion fails. That
these return an element value reference makes them closely akin to the feature
proposed in [@P3091R5].

Conclusions Drawn from Current Usage
------------------------------------

We conclude that returning `optional` from a `get` function is not in keeping
with the expected behavior of `get` in most contexts, and would thus make the
Standard inconsistent and confusing.

The name of the desired operation should reflect the fact that the lookup can
return without finding the value being sought; the current usage of `get`
generally implies the opposite.

P3091 offered `get_optional`, `lookup`, and `lookup_optional`, which failed to
evoke enthusiasm.  Interest at the time was focused on semantics, with
discussion of names felt by many to be a distraction.  The only criterion
mentioned for a good name was "short, which seems to the authors secondary to
descriptive clarity.

The only alternative seriously considered in Sofia was `lookup`, which was
rejected by weak consensus. The goal of this paper is to propose a short list
of alternative names which meet the criteria of being short, descriptive of the
functionality, and consistent (or at least not inconsistent) the names of other
value-returning functions that can return failure through a non-exception path.


Proposed Change
===============

This is a multipart proposal. The parts can be polled separately.

Part 1: Change `@_map-like_@::get` to a different name in P3091
---------------------------------------------------------------

Although we use the term _bikeshed_ to imply that naming discussions are of
minor importance, names do matter a lot. Naming discussions should not be an
exercise of throwing a bunch of names in the air and voting on based on gut
reactions. We propose that LEWG choose among the three alternatives below. Each
alternative includes some discussion of its advantages and disadvantages. When
polling the alternatives, we should pay attention not only to the name that
receives the most positive votes, but the strength of opposition to any
specific name.

+-------------------+-------------------------------------------------------+
| Name              | Discussion                                            |
+===================+=======================================================+
| `try_at`          | (Nathan's preference) Implies `at`-like functionality |
|                   | Pablo doesn't like it because `at` already has a      |
|                   | different failure mode (exceptions), so `try_at`      |
|                   | could cause confusion regarding which is which.       |
+-------------------+-------------------------------------------------------+
| `lookup`          | (Pablo's preference) Does not imply success. Simple.  |
|                   | Slightly strange name if we were to use it for        |
|                   | integral indexes in containers like `vector` and      |
|                   | `deque`.                                              |
+-------------------+-------------------------------------------------------+
| `try_lookup`      | Makes it explicit that failure is possible. Pablo     |
|                   | considers that redundant in that `lookup` does not    |
|                   | imply success, but is otherwise not opposed.          |
+-------------------+-------------------------------------------------------+

The operation should not throw unless a user-supplied functor (comparison,
hash, etc) throws.

The feature test macro should be renamed to match the new chosen name.

This change is targeted for C++29. Note that P3091 has not yet been voted into
the working paper, so this proposal would not change anything in the C++26
DIS.

Part 2: Add a Similar Member Function to Random-access Containers
-----------------------------------------------------------------

Conceptually, `myVector[@_index_@]` is a lookup operation that could fail if
`@_index_@` is out of range. Rather than depend on a user precondition check or
an exception from `at`, we can consider adding a `get`-like function to
`vector`, `inplace_vector`, and `deque`. Such a function might be especially
handy in generic code.

We therefore propose that a `get`-like member function, using whatever name is
chosen in Part 1, be added to random-access containers (or any container
exposing an `operator[](@_index_@)` or `at(@_index_@)` function.

Alternatives considered
=======================

The names `get_optional` and `lookup_optional` meet the stated criteria. If
there is sufficient interest, they can be considered as well as the three
proposed.

Wording
=======

Part 1
------

Generate a new revision of P3091, replacing the name `get` with whatever name
is selected by LEWG.

If P3091 is voted into the WD before this paper is forwarded to LWG, the change
would apply to the `get` functions in the associative containers in the WD,
instead.

Part 2
------

Wording for Part 2 of this proposal is relative to the 2025-12 Working Draft,
[@N5032].

Let _newname_ be `get`, `try_at`, `lookup`, or `try_lookup`, as determined in
part 1, above.

At the end of [sequence.reqmts]{.sref}, add:

::: add

>| `a.`_newname_`(n)`

>> _Result_ (`R`): `optional<mapped_type&>`; `optional<const mapped_type&>` for
>> constant `a`.

>> _Returns_: `n < a.size() ? R{a[n]} : R{}`

>> _Throws_: nothing

>> _Remarks_: Required for `basic_string`, `array`, `deque`, `inplace_vector`,
>> and `vector`.

:::

In [deque.overview]{.sref}, add the _newname` members:

>|    `//` _element access_
>|    `constexpr reference operator[](size_type n);`
>|    `constexpr const_reference operator[](size_type n) const;`
>|    `constexpr reference at(size_type n);`
>|    `constexpr const_reference at(size_type n) const;`
::: add
>|    `constexpr optional<mapped_type&> @_newname_@(size_type n);`
>|    `constexpr optional<const mapped_type&> @_newname_@(size_type n) const;`
:::
>|    `constexpr reference front();`
>|    `constexpr const_reference front() const;`
>|    `constexpr reference back();`
>|    `constexpr const_reference back() const;`

Make similar changes in [basic.string.general]{.sref},
[array.overview]{.sref}, [inplace.vector.overview]{.sref}, and
[vector.overview]{.sref}.

---
references:
  - id:
    citation-label:
    author:
    title: ""
    URL:
---
